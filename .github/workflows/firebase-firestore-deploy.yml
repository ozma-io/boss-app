name: Deploy Firebase Firestore

on:
  push:
    branches:
      - main
    paths:
      - 'firestore.rules'
      - 'firestore.indexes.json'

jobs:
  deploy:
    name: Deploy Firestore Rules & Indexes
    runs-on: ubuntu-latest
    env:
      FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
    steps:
      - name: Check for FIREBASE_TOKEN
        run: |
          if [ -z "$FIREBASE_TOKEN" ]; then
            echo "You must provide a FIREBASE_TOKEN secret in this repo's secrets."
            echo "Generate it with: firebase login:ci"
            exit 1
          fi

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Deploy Firestore to Firebase
        run: |
          npm install -g firebase-tools
          firebase deploy --only firestore --token "$FIREBASE_TOKEN" --non-interactive --force



